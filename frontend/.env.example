# Copy to .env.local for local overrides (not needed for normal local dev,
# since Vite's dev proxy already forwards /api -> localhost:8000).
#
# In Vercel, set this as a Project Environment Variable pointing at your
# deployed backend, e.g.:
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=
